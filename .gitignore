blender-3.6.9
bullet_src
# Bullet 2.75 reference source — read when porting solver behaviour (MMD runs
# this version). Kept locally, not vendored: it is upstream's tree, not ours.
bullet-2.75-rc3
# Local Claude Code settings
.claude/

# Internal design docs / handoff notes — kept locally, not published. Public info lives
# in the root README.
docs/

# Engine's npm README is generated from the root README at publish time (prepublishOnly).
engine/README.md

# Blender preset reference dumps + extraction scripts (used during shader porting,
# kept locally for future material work; recoverable from git history if needed)
m_graphs.json
world_dump.json
lights.json
仿深空之眼渲染预设v1.0_by_小绿毛猫_material_graph_dump.json
rendering.txt
graph_traces.txt
trace_graphs.py
scripts/dump_material_graphs.py
scripts/dump_scene_world.py
# Commercial Unity animation pack — raw FBX sources are never committed/redistributed.
# The converted VMDs (vmd/) ship with the demo; regenerate via reze-rig's fbx2vmd.
web/public/unity-fbx-locomotion/Animations/

# Blender source, unpacked for reference while porting node semantics to 5.2.
# READ, never vendored: the port lives in our own WGSL, and the licences differ.
blender-5.2.0/
blender-*.tar.xz

# Reference Blender projects — read to learn what to port, never redistributed.
*.blend
*.blend1

# Test fixtures that are someone else's model. MMD models carry
# redistribution terms; the round-trip test skips when this is absent.
engine/tests/fixtures/*.pmx
